apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: crypto-payments
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`pay.oclawbox.com`)
      kind: Rule
      services:
        - name: crypto-payments
          port: 3003
  tls: {}
